/* =====================================================================
   LAXTLINE — js/03-gallery-config.js
   ---------------------------------------------------------------------
   PURPOSE: tiny config / status marker. The gallery items are written
   directly into index.html (not generated by JS), so there is nothing
   to build here — this just logs a confirmation to the console so you
   can verify the gallery loaded. Safe to keep or remove.
   ===================================================================== */

// AutoGallery disabled — all items hardcoded above from All Projects folder
console.log('[Gallery] 71 items loaded from All Projects folder.');
